class Options
{
    static autofill = true;
    static strokeColor ="rgba(0,0,0,1);";
    static fillColor = "rgba(100,0,0,1);";
    constructor()
    {
        
    };
    static initialize()
    {
        // Get stroke width
        //test.match(/(?<=stroke-width=)(".*?")/)[0].replace(/\"/g,"'");

        // Get fill color
        //test.match(/(?<=fill=)(".*?")/)[0].replace(/\"/g,"'");

        // Get stroke color
        //test.match(/(?<=stroke=)(".*?")/)[0].replace(/\"/g,"'");

    }
}